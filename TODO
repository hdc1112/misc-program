`if possible, write programs for:
1. n numbers, find n-1 numbers such that multiplication is the biggest
2. n numbers, find three numbers such that they increasing
3. n numbers, find two numbers, such that aj > ai and j-i is the biggest

`write merge sort by two methods: extra space, and swap method:

`think about the gold pot problem, google's interview question:

`1-100 rotate unknown bits, find the smallest, O(lgn):

`think about 54321's previous, e.g. 54312, 12345 doesn't have a prev:

`think about using random(1-5) to get random(1-7)
(DONE)

`10 mice eat 1000 bottle pills, only one bottle is deadly. find it:
(DONE)
